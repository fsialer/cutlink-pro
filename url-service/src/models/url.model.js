class Url {
    constructor(id, short_code, url, clicks, expires_at, owner_id) {
        this.id = id;
        this.short_code = short_code;
        this.url = url;
        this.clicks = clicks;
        this.expires_at = expires_at;
        this.owner_id = owner_id;
    }
}